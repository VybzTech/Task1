import React from "react";

const Upload = () => {
  return (
    <div>
      <input
        type="file"
        name="Upload Image"
        placeholder="Upload Cover Image"
        id="Upload Image"
      />
    </div>
  );
};

export default Upload;
